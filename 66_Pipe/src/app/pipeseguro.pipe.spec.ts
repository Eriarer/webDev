import { PipeseguroPipe } from './pipeseguro.pipe';

describe('PipeseguroPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeseguroPipe();
    expect(pipe).toBeTruthy();
  });
});
